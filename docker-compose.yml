version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: mtgdecklab
      POSTGRES_USER: mtgdecklab
      POSTGRES_PASSWORD: mtgdecklab_dev
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: